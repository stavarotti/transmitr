import Ember from 'ember';

export default Ember.Service.extend({
  /**
   * Whether the navigation drawer is open.
   * 
   * @public
   * @property isNavigationDrawerOpen
   * @type {boolean}
   */
  isNavigationDrawerOpen: false
});
