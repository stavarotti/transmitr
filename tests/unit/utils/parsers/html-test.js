import parsersHtml from 'transmittr/utils/parsers/html';
import { module, test } from 'qunit';

module('Unit | Utility | parsers/html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parsersHtml();
  assert.ok(result);
});
