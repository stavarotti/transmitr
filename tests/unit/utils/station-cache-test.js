import stationCache from 'transmittr/utils/station-cache';
import { module, test } from 'qunit';

module('Unit | Utility | station cache');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stationCache();
  assert.ok(result);
});
