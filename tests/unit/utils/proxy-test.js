import proxy from 'transmittr/utils/proxy';
import { module, test } from 'qunit';

module('Unit | Utility | proxy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = proxy();
  assert.ok(result);
});
